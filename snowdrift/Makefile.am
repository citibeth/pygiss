# this is example-file: Makefile.am

# the subdirectories of the project to go into
SUBDIRS = 	\
			include \
			slib \
			sbin \
			pyext

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = snowdrift.pc
